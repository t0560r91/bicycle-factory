package facility

class BicycleInspector

    //     val assemplyParts: List[Part] = List(handle, brakeLever, seat, frontSporacket, pedal, frontWheel, rearWheel, rearSporacket,rearbrake, chain)
    //     for (part <- assemplyParts) {
    //         if (part.inspectConnections()) {
    //             println(f"$part is connected")
    //         } else {
    //             println(f"$part is not connected")
    //             throw new Exception("Error")
    //         }


    // def inspectAssembly(): Boolean = {
    //     val assemplyParts: List[Part] = List(handle, brakeLever, seat, frontSporacket, pedal, frontWheel, rearWheel, rearSporacket,rearbrake, chain)
    //     for (part <- assemplyParts) {
    //         if (part.inspectConnections()) {
    //             println(f"$part is connected")
    //         } else {
    //             println(f"$part is not connected")
    //             throw new Exception("Error")
    //         }
    //     }
    // }